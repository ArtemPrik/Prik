var arr = [10, 7, 8, 2, 5, 4, 1, 3, 6, 9, 32, 12, 65]

var i var j
for(i = 0, i < arr.length, i+=1) {
    for(j = 0, j < arr.length - 1, j+=1) {
        if (arr[j] > arr[j + 1]) {
            var n = arr[j]
            arr[j] = arr[j + 1]
            arr[j + 1] = n
        }
    }
}

println arr
